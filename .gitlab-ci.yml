---
build:
  stage: build
  image: node:24
  before_script:
    - npm install --global corepack@latest
    - corepack enable
    - pnpm config set store-dir .pnpm-store
  script:
    - pnpm install
  cache:
    key:
      files:
        - pnpm-lock.yaml
    paths:
      - .pnpm-store
